import React, {useEffect, useMemo, useRef, useState} from 'react'
import {createRoot} from 'react-dom/client'
import {AreaChart, Area, BarChart, Bar, Cell, PieChart, Pie, ResponsiveContainer, Tooltip, XAxis, YAxis} from 'recharts'
import {ArrowUpRight, Check, ChevronLeft, CircleAlert, Edit3, LogOut, Plus, RefreshCw, Search, Trash2, X} from 'lucide-react'
import {configured, supabase} from './supabase'
import {demoHoldings, funds, stocks} from './data'
import './style.css'

const rupee = n => new Intl.NumberFormat('en-IN',{style:'currency',currency:'INR',maximumFractionDigits:0}).format(Number(n)||0)
const decimal = n => new Intl.NumberFormat('en-IN',{maximumFractionDigits:2}).format(Number(n)||0)
const uid = () => crypto.randomUUID()
const value = h => Number(h.units)*Number(h.current_price)
const invested = h => Number(h.units)*Number(h.avg_cost)
const change = h => (value(h)-invested(h))/invested(h)*100

function Count({value: target}) { const [shown,setShown]=useState(target); const before=useRef(target); useEffect(()=>{let start, frame; const from=before.current, diff=target-from; const run=t=>{start??=t; const p=Math.min((t-start)/700,1); setShown(from+diff*(1-Math.pow(1-p,3))); if(p<1) frame=requestAnimationFrame(run); else before.current=target}; frame=requestAnimationFrame(run); return()=>cancelAnimationFrame(frame)},[target]); return <>{rupee(shown)}</> }

function BgVideo({src}){return <div className="bgVideo"><video autoPlay muted loop playsInline src={src}/><div className="bgOverlay"/></div>}

function MusicPlayer({src}){const ref=useRef(null),[on,setOn]=useState(false);useEffect(()=>{const tryPlay=()=>{if(ref.current&&!on){ref.current.volume=.35;ref.current.play().then(()=>setOn(true)).catch(()=>{})}document.removeEventListener('click',tryPlay)};document.addEventListener('click',tryPlay);return()=>document.removeEventListener('click',tryPlay)},[on]);const toggle=()=>{if(!ref.current)return;if(ref.current.paused){ref.current.play();setOn(true)}else{ref.current.pause();setOn(false)}};return <><audio ref={ref} loop preload="auto" src={src}/><button className="musicToggle" onClick={toggle}>{on?'♫ Sound on':'♪ Sound off'}</button></>}

function App(){
 const [session,setSession]=useState(null),[screen,setScreen]=useState('login'),[holdings,setHoldings]=useState(demoHoldings),[history,setHistory]=useState([]),[notice,setNotice]=useState(''),[refreshing,setRefreshing]=useState(false),[modal,setModal]=useState(null)
 useEffect(()=>{if(!configured)return; supabase.auth.getSession().then(({data})=>setSession(data.session)); const {data:{subscription}}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s)); return()=>subscription.unsubscribe()},[])
 useEffect(()=>{if(!session)return; load(); async function load(){const [{data:h},{data:p},{data:hist}]=await Promise.all([supabase.from('holdings').select('*').order('updated_at'),supabase.from('profiles').select('setup_complete').single(),supabase.from('portfolio_history').select('*').order('recorded_at').limit(30)]); setHoldings(h||[]);setHistory(hist||[]);setScreen(p?.setup_complete?'dashboard':'onboard')}} , [session])
 const persist=async next=>{setHoldings(next);if(configured&&session){const {error}=await supabase.from('holdings').upsert(next.map(({id,...h})=>({...h,id:id?.startsWith('d')?undefined:id,user_id:session.user.id})));if(error)setNotice(error.message)}}
 const login=async provider=>{if(!configured){setNotice('Add Supabase environment variables to enable real OAuth. Showing the product flow in preview mode.');setScreen('onboard');return} const {error}=await supabase.auth.signInWithOAuth({provider,options:{redirectTo:location.origin}});if(error)setNotice(error.message)}
 const refresh=async()=>{setRefreshing(true);setNotice(''); try {const stocksOnly=holdings.filter(h=>h.type==='stock'&&h.symbol&&h.symbol.trim()); const fundsWithCode=holdings.filter(h=>h.type==='fund'&&/^\d+$/.test(String(h.symbol).trim())); const fundsManual=holdings.filter(h=>h.type==='fund'&&!/^\d+$/.test(String(h.symbol).trim())); let stockQuotes=[],stockAttempted=0; if(stocksOnly.length){stockAttempted=stocksOnly.length; if(!import.meta.env.VITE_TWELVE_DATA_API_KEY){stockAttempted=0}else{const results=await Promise.all(stocksOnly.map(async h=>{try{const r=await fetch(`https://api.twelvedata.com/price?symbol=${encodeURIComponent(h.symbol.trim())}:NSE&apikey=${import.meta.env.VITE_TWELVE_DATA_API_KEY}`);const d=await r.json();if(!d.price)return null;return [h.id,Number(d.price)]}catch{return null}})); stockQuotes=results.filter(Boolean)}} const fundResults=await Promise.all(fundsWithCode.map(async h=>{try{const r=await fetch(`https://api.mfapi.in/mf/${encodeURIComponent(String(h.symbol).trim())}/latest`);const d=await r.json();const nav=Number(d?.data?.[0]?.nav);if(!nav)return null;return [h.id,nav]}catch{return null}})); const fundQuotes=fundResults.filter(Boolean); const map=new Map([...stockQuotes,...fundQuotes]);const next=holdings.map(h=>map.has(h.id)?{...h,current_price:map.get(h.id)}:h); await persist(next); await record(next); const stockFailed=stockAttempted-stockQuotes.length; const fundFailed=fundsWithCode.length-fundQuotes.length; const parts=[]; if(stockAttempted)parts.push(`${stockQuotes.length} of ${stockAttempted} stock(s)`); if(fundsWithCode.length)parts.push(`${fundQuotes.length} of ${fundsWithCode.length} fund NAV(s)`); let msg=parts.length?`Refreshed ${parts.join(' · ')}.`:'Nothing to refresh — add a holding first.'; if(!import.meta.env.VITE_TWELVE_DATA_API_KEY&&stocksOnly.length)msg+=' Add a Twelve Data key to refresh stocks.'; if(stockFailed>0)msg+=` ${stockFailed} stock symbol(s) had no data.`; if(fundFailed>0)msg+=` ${fundFailed} fund(s) had no NAV data.`; if(fundsManual.length)msg+=` ${fundsManual.length} fund(s) added manually stay manual — re-add via "Search all AMFI schemes" for auto NAV.`; setNotice(msg)}catch(e){setNotice(e.message)}finally{setRefreshing(false)}}
 const record=async list=>{const total=list.reduce((s,h)=>s+value(h),0), investedTotal=list.reduce((s,h)=>s+invested(h),0);const point={value:total,invested:investedTotal,recorded_at:new Date().toISOString()};setHistory(x=>[...x,point]); if(configured&&session) await supabase.from('portfolio_history').insert({...point,user_id:session.user.id})}
 const saveHolding=async h=>{const next=modal?.id?holdings.map(x=>x.id===h.id?h:x):[...holdings,{...h,id:uid()}];await persist(next);setModal(null)}
 const remove=async id=>{const next=holdings.filter(h=>h.id!==id);setHoldings(next);if(configured&&session)await supabase.from('holdings').delete().eq('id',id)}
 if(screen==='login')return <><MusicPlayer src="https://res.cloudinary.com/x1e5dtb1/video/upload/v1787532150/bg-music.mp3"/><Login onLogin={login} notice={notice}/></>
 if(screen==='onboard')return <><MusicPlayer src="https://res.cloudinary.com/x1e5dtb1/video/upload/v1787532150/bg-music.mp3"/><Onboard initial={holdings.filter(h=>!h.id.startsWith('d'))} onDone={async selected=>{await persist(selected);if(configured&&session)await supabase.from('profiles').upsert({id:session.user.id,setup_complete:true});setScreen('dashboard')}} onBack={()=>setScreen('login')}/></>
 return <><MusicPlayer src="https://res.cloudinary.com/x1e5dtb1/video/upload/v1787532150/bg-music.mp3"/><Dashboard holdings={holdings} history={history} notice={notice} refreshing={refreshing} onRefresh={refresh} onLogout={async()=>{if(configured)await supabase.auth.signOut();setScreen('login')}} onEdit={setModal} onDelete={remove} onAdd={()=>setModal({type:'stock'})}>{modal&&<HoldingModal initial={modal.id?modal:{...modal,units:'',avg_cost:'',current_price:''}} onClose={()=>setModal(null)} onSave={saveHolding}/>}</Dashboard></>
}

function Login({onLogin,notice}){return <main className="login"><BgVideo src="https://res.cloudinary.com/x1e5dtb1/video/upload/v1787531589/login-bg.mp4"/><div className="brand">LEDGER <i>ALPHA</i></div><section className="loginCard"><span className="eyebrow">PRIVATE INVESTMENT LEDGER</span><h1>Every rupee.<br/><em>In its place.</em></h1><p>A personal ledger for the Indian investor who prefers clarity over noise.</p><button className="oauth google" onClick={()=>onLogin('google')}>G <span>Continue with Google</span></button><button className="oauth x" onClick={()=>onLogin('twitter')}>𝕏 <span>Continue with X</span></button>{notice&&<p className="notice"><CircleAlert/> {notice}</p>}<small>By continuing, you agree to manage your own investment records.</small></section><div className="loginMark">₹<br/><span>EST. 2026</span></div></main>}

function Onboard({initial,onDone,onBack}){const [tab,setTab]=useState('stock'),[query,setQuery]=useState(''),[chosen,setChosen]=useState(initial),[remote,setRemote]=useState([]),[lookup,setLookup]=useState('');const catalog=tab==='stock'?stocks:funds;const local=catalog.filter(x=>(x.name+x.symbol).toLowerCase().includes(query.toLowerCase()));const matches=[...local,...remote.filter(x=>!local.some(y=>y.symbol===x.symbol))];const toggle=x=>setChosen(c=>c.some(v=>v.symbol===x.symbol)?c.filter(v=>v.symbol!==x.symbol):[...c,{...x,id:uid(),units:'',avg_cost:'',current_price:''}]);const edit=(symbol,key,val)=>setChosen(c=>c.map(v=>v.symbol===symbol?{...v,[key]:val}:v));const findStock=async()=>{if(!query.trim())return;if(!import.meta.env.VITE_TWELVE_DATA_API_KEY){setLookup('Add a Twelve Data API key to search the full exchange universe. You can also add it from the dashboard.');return}try{setLookup('Searching exchange…');const r=await fetch(`https://api.twelvedata.com/symbol_search?symbol=${encodeURIComponent(query)}&apikey=${import.meta.env.VITE_TWELVE_DATA_API_KEY}`);const d=await r.json();const found=(d.data||[]).filter(x=>/NSE|BSE|India/i.test(x.exchange||x.country||'')).map(x=>({name:x.instrument_name||x.symbol,symbol:x.symbol,type:'stock'}));setRemote(found);setLookup(found.length?'':'No Indian listings found.')}catch{setLookup('Lookup failed. Check your connection and retry.')}}; const findFund=async()=>{if(!query.trim())return;try{setLookup('Searching the full AMFI fund catalogue…');const r=await fetch(`https://api.mfapi.in/mf/search?q=${encodeURIComponent(query)}`);const d=await r.json();const found=(Array.isArray(d)?d:[]).slice(0,40).map(x=>({name:x.schemeName,symbol:String(x.schemeCode),type:'fund'}));setRemote(found);setLookup(found.length?'':'No matching scheme found.')}catch{setLookup('Lookup failed. Check your connection and retry.')}}; const ready=chosen.length&&chosen.every(x=>x.units&&x.avg_cost&&x.current_price);return <main className="onboard"><BgVideo src="https://res.cloudinary.com/x1e5dtb1/video/upload/v1787531570/onboard-bg.mp4"/><button className="back" onClick={onBack}><ChevronLeft/> Back</button><header><span className="eyebrow">PORTFOLIO SETUP · ONE TIME</span><h1>Build your <em>ledger.</em></h1><p>Select what you hold today. You can edit it any time.</p></header><div className="tabs"><button className={tab==='stock'?'active':''} onClick={()=>setTab('stock')}>Stocks <b>{chosen.filter(x=>x.type==='stock').length}</b></button><button className={tab==='fund'?'active':''} onClick={()=>setTab('fund')}>SIP / Mutual Funds <b>{chosen.filter(x=>x.type==='fund').length}</b></button></div><label className="search"><Search/><input autoFocus placeholder={`Search ${tab==='stock'?'NSE & BSE shares':'mutual funds'}`} value={query} onChange={e=>{setQuery(e.target.value);setRemote([]);setLookup('')}}/>{tab==='stock'&&<button type="button" onClick={findStock}>Full exchange lookup</button>}{tab==='fund'&&<button type="button" onClick={findFund}>Search all AMFI schemes</button>}</label>{lookup&&<p className="lookup">{lookup}</p>}<div className="selectionList">{matches.map(x=>{const found=chosen.find(v=>v.symbol===x.symbol);return <div className={'selectRow '+(found?'selected':'')} key={x.symbol}><button aria-label={'Select '+x.name} className="check" onClick={()=>toggle(x)}>{found&&<Check/>}</button><div className="assetName"><b>{x.name}</b><code>{x.symbol}</code></div>{found&&<div className="inlineFields"><label>{x.type==='stock'?'Quantity':'Units'}<input type="number" value={found.units} onChange={e=>edit(x.symbol,'units',e.target.value)}/></label><label>Avg {x.type==='stock'?'buy price':'NAV'}<input type="number" value={found.avg_cost} onChange={e=>edit(x.symbol,'avg_cost',e.target.value)}/></label><label>Current {x.type==='stock'?'price':'NAV'}<input type="number" value={found.current_price} onChange={e=>edit(x.symbol,'current_price',e.target.value)}/></label></div>}</div>})}</div><footer className="onboardFoot"><span>{chosen.length} holding{chosen.length!==1?'s':''} selected</span><button className="gold" disabled={!ready} onClick={()=>onDone(chosen)}>Build my portfolio <ArrowUpRight/></button></footer></main>}

function Dashboard({holdings,history,notice,refreshing,onRefresh,onLogout,onEdit,onDelete,onAdd,children}){const [view,setView]=useState('all');const shown=view==='all'?holdings:holdings.filter(h=>h.type===view);const total=shown.reduce((s,h)=>s+value(h),0), cost=shown.reduce((s,h)=>s+invested(h),0), gain=total-cost; const stocksValue=holdings.filter(h=>h.type==='stock').reduce((s,h)=>s+value(h),0); const chartHistory=(history.length?history:[{value:cost,recorded_at:new Date(Date.now()-864e5).toISOString()},{value:total,recorded_at:new Date().toISOString()}]).map(x=>({...x,date:new Date(x.recorded_at).toLocaleDateString('en-IN',{day:'numeric',month:'short'})}));const allocation=shown.map(h=>({name:h.symbol,value:value(h),fill:h.type==='stock'?'#c9a227':'#2e8b7f'}));return <main className="dash"><BgVideo src="https://res.cloudinary.com/x1e5dtb1/video/upload/v1787531490/dashboard-bg.mp4"/><div className="ticker"><div className="tickerTrack">{[...holdings,...holdings].map((h,i)=><span key={i}><b>{h.symbol}</b> {rupee(h.current_price)} <i className={change(h)>=0?'up':'down'}>{change(h)>=0?'+':''}{change(h).toFixed(2)}%</i></span>)}</div></div><nav><div className="brand">LEDGER <i>ALPHA</i></div><div className="viewTabs"><button className={view==='all'?'on':''} onClick={()=>setView('all')}>Combined</button><button className={view==='stock'?'on':''} onClick={()=>setView('stock')}>Stocks</button><button className={view==='fund'?'on':''} onClick={()=>setView('fund')}>SIP</button></div><span className="navText">PERSONAL PORTFOLIO / INDIA</span><button className="logout" onClick={onLogout}><LogOut/> Log out</button></nav><section className="hero"><div><span className="eyebrow">NET PORTFOLIO VALUE</span><h1><Count value={total}/></h1><p className={gain>=0?'up':'down'}>{gain>=0?'▲':'▼'} {rupee(Math.abs(gain))} <small>({Math.abs(gain/cost*100||0).toFixed(2)}%) all time</small></p></div><div className="heroActions"><button className="refresh" onClick={onRefresh} disabled={refreshing}><RefreshCw className={refreshing?'spin':''}/> {refreshing?'Refreshing…':'Refresh live prices'}</button><small>Stocks via market-data API · Fund NAVs auto-refresh if added via AMFI search</small></div></section>{notice&&<div className="notice dashboardNotice"><CircleAlert/>{notice}<button onClick={onRefresh}>Retry</button></div>}<section className="cards"><Stat label="TOTAL INVESTED" value={rupee(cost)}/><Stat label="STOCKS VALUE" value={rupee(stocksValue)} tone="gold"/><Stat label="SIP / FUND VALUE" value={rupee(total-stocksValue)} tone="teal"/><Stat label="NET GAIN / LOSS" value={rupee(gain)} tone={gain>=0?'green':'red'}/></section><section className="grid"><Panel title="Portfolio trajectory" sub="A new data point is recorded with each price refresh." cls="trajectory"><ResponsiveContainer><AreaChart data={chartHistory}><defs><linearGradient id="goldFill" x1="0" x2="0" y1="0" y2="1"><stop stopColor="#c9a227" stopOpacity=".32"/><stop offset="1" stopColor="#c9a227" stopOpacity="0"/></linearGradient></defs><XAxis dataKey="date"/><YAxis hide/><Tooltip formatter={v=>rupee(v)}/><Area type="monotone" dataKey="value" stroke="#c9a227" strokeWidth={2} fill="url(#goldFill)"/></AreaChart></ResponsiveContainer></Panel><Panel title="Allocation" sub="Current value by holding" cls="allocation"><ResponsiveContainer><PieChart><Pie data={allocation} dataKey="value" innerRadius="56%" outerRadius="78%" paddingAngle={3}>{allocation.map((x,i)=><Cell key={i} fill={x.fill}/>)}</Pie><Tooltip formatter={v=>rupee(v)}/></PieChart></ResponsiveContainer><div className="legend"><i/><span>Stocks</span><i className="teal"/><span>Funds</span></div></Panel><Panel title="Return by holding" sub="Unrealised return percentage" cls="returns"><ResponsiveContainer><BarChart data={shown.map(h=>({name:h.symbol,return:change(h)}))}><XAxis dataKey="name" interval={0} tick={{fontSize:9}}/><YAxis hide/><Tooltip formatter={v=>`${Number(v).toFixed(2)}%`}/><Bar dataKey="return" radius={[3,3,0,0]}>{shown.map((h,i)=><Cell key={i} fill={change(h)>=0?'#3fb68c':'#e2604b'}/>)}</Bar></BarChart></ResponsiveContainer></Panel></section><section className="ledger"><div className="sectionTitle"><div><span className="eyebrow">YOUR HOLDINGS</span><h2>The ledger</h2></div><button className="add" onClick={onAdd}><Plus/> Add holding</button></div><div className="tableWrap"><table><thead><tr><th>Instrument</th><th>Units</th><th>Avg. cost / NAV</th><th>Current price / NAV</th><th>Current value</th><th>Return</th><th/></tr></thead><tbody>{shown.map(h=><tr key={h.id}><td><b>{h.name}</b><code className={h.type}>{h.symbol} · {h.type==='fund'?'MANUAL NAV':'NSE'}</code></td><td>{decimal(h.units)}</td><td>{rupee(h.avg_cost)}</td><td><span className={h.type==='stock'?'pricePulse':''}>{rupee(h.current_price)}</span></td><td><b>{rupee(value(h))}</b></td><td className={change(h)>=0?'up':'down'}>{change(h)>=0?'+':''}{change(h).toFixed(2)}%</td><td className="rowActions"><button aria-label="Edit" onClick={()=>onEdit(h)}><Edit3/></button><button aria-label="Delete" onClick={()=>onDelete(h.id)}><Trash2/></button></td></tr>)}</tbody></table></div></section>{children}</main>}
function Stat({label,value,tone=''}){return <article className={'stat '+tone}><span>{label}</span><b>{value}</b></article>};function Panel({title,sub,cls,children}){return <article className={'panel '+cls}><h3>{title}</h3><p>{sub}</p>{children}</article>}
function HoldingModal({initial,onClose,onSave}){const [h,setH]=useState(initial);const change=(k,v)=>setH(x=>({...x,[k]:v}));return <div className="modal"><form onSubmit={e=>{e.preventDefault();onSave({...h,units:Number(h.units),avg_cost:Number(h.avg_cost),current_price:Number(h.current_price)})}}><button type="button" className="close" onClick={onClose}><X/></button><span className="eyebrow">{h.id?'EDIT HOLDING':'NEW HOLDING'}</span><h2>{h.id?'Update entry':'Add to ledger'}</h2><label>Name<input required value={h.name||''} onChange={e=>change('name',e.target.value)}/></label><div className="two"><label>Ticker / Fund code<input required value={h.symbol||''} onChange={e=>change('symbol',e.target.value)}/></label><label>Type<select value={h.type} onChange={e=>change('type',e.target.value)}><option value="stock">Stock</option><option value="fund">SIP / Fund</option></select></label></div><div className="two"><label>{h.type==='stock'?'Quantity':'Units'}<input required min="0" step="any" type="number" value={h.units} onChange={e=>change('units',e.target.value)}/></label><label>Average cost / NAV<input required min="0" step="any" type="number" value={h.avg_cost} onChange={e=>change('avg_cost',e.target.value)}/></label></div><label>Current price / NAV <small>{h.type==='fund'?'Manual NAV — update from your fund statement.':'Live refresh can overwrite this.'}</small><input required min="0" step="any" type="number" value={h.current_price} onChange={e=>change('current_price',e.target.value)}/></label><button className="gold" type="submit">Save holding <Check/></button></form></div>}
createRoot(document.getElementById('root')).render(<App/>)
